# React-Core-Concepts
hands-on projects built using **React.js** to strengthen core React concepts through practical implementation. Each project focuses on understanding how React works internally rather than just building UI.
